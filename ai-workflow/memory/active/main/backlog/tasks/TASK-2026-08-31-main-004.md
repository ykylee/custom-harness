---
id: TASK-2026-08-31-main-004
status: done
created_at: 2026-08-31
source_anchor: generic-task-2026-08-31-main-004
source_path: backlog/2026-08-31.md
kind: generic
---

# TASK-2026-08-31-main-004 — M7 7.2.0b grok 권한 모드 확정 (FR-1.5)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-31
- Owner: yklee
- Host:
- Host IP:
- Affected documents:
  - `docs/reference/harness-mcp-support.md`
  - `docs/design/adapter-contract.md`
  - `docs/requirements/fr1-harness-sessions.md`
  - `docs/roadmap/m7-orchestration.md`
  - `docs/roadmap/PROGRESS.md`

- Description: 7.2.1 에서 검출한 "grok ACP 가 MCP 툴 호출을 `session/request_permission` 없이 거절한다" 결함의 원인을 규명하고, MCP 툴이 승인 채널을 타면서 FR-1.5(승인·감사)도 유지되는 권한 모드를 확정한다. 7.2.3 의 선행.
- Completion criteria: 결함의 원인이 실측 근거와 함께 규명될 것
- Completion criteria: `--permission-mode` 모드별로 MCP 툴·내장 파괴적 툴의 승인 요청/실행 여부 행렬 확보
- Completion criteria: 어댑터가 선택한 모드를 명시 고정하고, 권한 모드 없이도 grok MCP 왕복이 성립할 것

## 🛠️ Implementation / Content

- Progress: `scripts/grok-permission-probe.mjs` 신설 — 모드마다 데몬 + 실물 grok 세션을 띄우고 목 게이트웨이가 한 턴 안에서 `search_tool` → `use_tool`(우리 MCP 툴) → 내장 쓰기 툴을 **고정 순서**로 호출시킨다(모드 간 자극 동일). 각 단계의 `permission_requested` 도달·실제 실행·잘림 사유를 기록.
- Progress: 원인 규명 — `grok inspect` 가 `Permissions Source: ~/.claude/settings.json (settings) 12 loaded` 를 찍는다. 측정 PC 의 해당 파일에 `permissions.defaultMode = "auto"` + Bash 전용 allow 12건이 있었고, grok 의 Claude 호환 import 가 이를 읽어 auto 모드로 들어갔다. auto 모드는 목록에 없는 툴을 *묻지 않고* 거절한다 — §3.1 홈 누수의 다른 증상.
- Progress: `GrokAdapter` 에 `permissionMode` 옵션 추가(`GrokPermissionMode` 타입, 기본 `'default'`, `'inherit'` 만 플래그 생략). `agent stdio` spawn 인자에 `--permission-mode <mode>` 를 항상 붙인다. `main.ts` 도 명시 지정.
- Progress: fake grok 픽스처가 `FAKE_GROK_ARGV_FILE` 로 spawn 인자를 기록하도록 확장 — 어댑터가 실제로 무엇을 붙였는지 테스트가 검증(3건 신규).
- Next session starting point: 7.2.2 툴 카탈로그 정본 정의(선행 전부 해소) 또는 7.1.1 주의 상태 1급화
- Remaining risks: 영속 승인(`always-allow`/`allow-edits-session`)의 UI 표기·감사 기록 미정 — FR-1.5 후속. 격리 `GROK_HOME` 덕에 영속 범위는 번들 데이터 안으로 한정된다
- Remaining risks: 모드 의미론은 grok 버전에 종속된다(1.0.5 → 1.0.13 사이에 이미 드리프트). 번들 grok 갱신 시 모드 행렬 재실측 필요

## ✅ Outcome

- Result: 확정 — `--permission-mode default` 명시 고정. 모드 행렬 실측 결과 **MCP 툴과 내장 파괴적 툴이 동시에 승인 대상이 되는 모드는 `default` 뿐**(`acceptEdits`·`dontAsk`·`plan` 은 MCP 툴을 묻지 않고 실행, `auto`·`bypassPermissions` 는 내장 쓰기까지 자동 허용). 원래 목표였던 "MCP 툴만 승인 대상" 보다 나은 상태다 — 자동 허용 정책은 우리 레이어(`autoApprove`)가 소유한다. 검증: `node scripts/mcp-probe.mjs --daemon --only grok` 이 **권한 우회 플래그 없이** `invoked=true returned=true` 로 통과. 부수 실측: 승인 옵션이 2종→3종(영속 노출), ACP `kind` 는 정확해 매핑 오류 없음 — adapter-contract §4 의 재실측 조건 발동. 게이트: typecheck·test(360 passed | 2 skipped)·lint·format 그린, `npm run smoke:nfr1` PASS.
