---
id: TASK-2026-08-31-main-005
status: done
created_at: 2026-08-31
source_anchor: generic-task-2026-08-31-main-005
source_path: backlog/2026-08-31.md
kind: generic
---

# TASK-2026-08-31-main-005 — M7 WP 7.1 주의 상태 1급화 (FR-9.1)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-31
- Owner: yklee
- Host:
- Host IP:
- Affected documents:
  - `docs/requirements/fr9-orchestration.md`
  - `docs/design/protocol-design.md`
  - `docs/design/workspace-model.md`
  - `docs/roadmap/m7-orchestration.md`
  - `docs/roadmap/PROGRESS.md`

- Description: "사용자가 봐야 할 세션"의 판단을 렌더러 로컬 계산에서 데몬의 단일 정책 모듈로 이관한다. 주의 상태를 세션 레코드의 영속 필드로 두고, 상태 변화를 이벤트로 발행하며, 버킷·알림이 그 하나만 소비하게 한다. 7.2.2 툴 카탈로그의 선행.
- Completion criteria: 데몬에 단일 정책 모듈이 존재하고 사유 우선순위가 테스트로 고정될 것
- Completion criteria: 주의 상태가 세션 레코드에 영속되고 데몬 재기동 후에도 조회될 것
- Completion criteria: 상태 변화 이벤트가 발행되고 렌더러의 로컬 계산이 제거될 것

## 🛠️ Implementation / Content

- Progress: 7.1.1 — `packages/daemon/src/attention.ts` 신설. `computeAttention(input, previous)` 이 사유를 `permission` > `error` > `finished` 순으로 고른다. 승인 대기는 확인 처리로 덮이지 않고, `running`/`initializing` 과 `canceled` 턴은 주의 대상이 아니며, 같은 사유가 이어지면 `attentionTimestamp` 를 보존한다. `attentionChanged()` 로 헛된 이벤트를 막는다.
- Progress: 7.1.1 — `SessionMeta`/`SessionSummary` 에 `attentionTimestamp` 추가(사유 enum 순서도 우선순위와 일치하게 정렬). `SessionManager` 가 턴 종료·상태 전이·승인 요청/해소·확인 처리·새 프롬프트에서 `refreshAttention()` 을 부르고, 변화가 있을 때만 meta 영속 + 이벤트 발행. 재기동 시 영속 값에서 `attentionAcknowledged` 를 복원.
- Progress: 7.1.2 — `attention_changed` 와이어 이벤트(데몬 소유 — 어댑터 유니온에는 없음, `user_message` 와 같은 층) + `session.attention.ack` RPC(멱등) + `summarize()` 가 주의 3필드를 항상 실어 보낸다(재접속 조회 경로).
- Progress: 7.1.3 — 렌더러 `bucketOf` 가 `status`/`pendingPermissions` 로 재계산하던 규칙을 제거하고 `requiresAttention`/`attentionReason` 만 소비('확인 필요' 버킷 추가, 주의 버킷만 색 강조). 알림은 `turn_completed`/`permission_requested` 를 각자 해석하던 경로에서 `attention_changed` 단일 소비로 이관. 세션을 열면 ack 를 보낸다.
- Next session starting point: 7.2.2 툴 카탈로그 정본 정의 (선행 7.1.1 해소) → 7.2.3 노출 2경로
- Remaining risks: 트레이 배지·자동 승인 정책은 아직 이 상태를 직접 소비하지 않는다(현재 배지는 세션 목록 갱신으로 간접 반영) — 트레이는 셸 트랙에서 배선 필요
- Remaining risks: `attentionTimestamp` 기반 정렬("오래 기다린 순")은 UI 에 아직 노출하지 않았다

## ✅ Outcome

- Result: WP 7.1 완료. 주의 상태가 데몬 정본이 됐고 렌더러 로컬 계산은 제거됐다. 클라이언트 부재 구간 보존을 데몬 재기동 테스트로 확인(같은 저장소 위에 새 매니저를 세워 `requiresAttention: true, attentionReason: 'finished'` 조회). 승인 대기가 확인 처리로 사라지지 않는다는 정책도 테스트로 고정. 게이트: typecheck·test(**376 passed | 2 skipped**, +18건)·lint·format 그린, `npm run smoke:nfr1` PASS.
