---
id: TASK-2026-08-31-main-008
status: done
created_at: 2026-08-31
source_anchor: generic-task-2026-08-31-main-008
source_path: backlog/2026-08-31.md
kind: generic
wbs: 7.2.3
---

# TASK-2026-08-31-main-008 — M7 7.2.3 역방향 툴 노출 — 데몬 MCP stdio 서버 (omp·grok)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-31
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `docs/roadmap/m7-orchestration.md`
  - `docs/design/reverse-tool-catalog.md`
  - `docs/reference/harness-mcp-support.md`
  - `docs/roadmap/PROGRESS.md`

- Description: 카탈로그 10종을 omp·grok 에 MCP 로 노출한다. 데몬 소유 stdio 서버가 데몬 WS 로 되붙어 툴을 처리하고, read 5종을 실제 동작시킨다. write 5종은 승인 경로 미구현이므로 명시적 거부(7.2.4 로 이월). 등록은 grok=grok mcp add 위임 / omp=격리 홈 mcp.json + tools.xdev=false, omp rpc 경로의 MCP 준비 완료 게이트 포함. pi 확장은 후속.
- Completion criteria: mcp-probe --daemon 로 omp·grok 양쪽에서 왕복(등록→노출→호출→결과 반영) PASS / read 5종이 실제 데몬 상태를 반환 / write 5종은 승인 미구현 오류로 거부되고 테스트가 이를 고정 / omp 1턴째 노출(준비 완료 게이트) 확인 / 게이트(typecheck·test·lint·format) 그린 / NFR-1 스모크 PASS

## 🛠️ Implementation / Content

- Progress: `2026-09-01 00:18` 기준 카탈로그 10종을 omp·grok 에 MCP 로 노출한다. read 5종 동작, write 5종은 승인 채널 부재로 명시 거부(7.2.4 이월).
- Next session starting point: 7.2.3b pi 확장(pi.registerTool) → 7.2.4 승인 채널·opt-in·재귀 상한·감사 로그·서버명 선점 탐지
- Remaining risks: 완료 기준의 'omp 1턴째 노출 확인' 은 **구조적으로 불가**로 판명 — omp 는 MCP 서버를 첫 턴이 시작될 때 띄운다(세션 생성 후 8초 대기해도 프로세스 없음). 게이트를 2턴째 이후 경합 방지로 재정의했고 설계서·PROGRESS 에 반영. 별건: 7.2.2 의 z.object 가 실제로는 미지 키를 제거만 해 '엄격' 주장이 거짓이었다 — z.strictObject 로 교정(e2e 가 검출).

## ✅ Outcome

- Result: packages/daemon/src/mcp/ 신설(server·tools·client·registration·main). 하네스가 spawn 하는 stdio 서버가 데몬 WS 로 되붙는다 — 토큰은 등록 파일에 적지 않고 데이터 디렉토리에서 직접 읽는다(회전·평문 노출 회피). CUSTOM_HARNESS_HOME 은 항상 명시(홈 격리된 하네스의 자식이라 homedir() 로는 못 찾음). 등록: omp=격리 홈 mcp.json + tools.xdev=false / grok=grok mcp add 위임. terminal.read RPC 신설(attach 와 달리 슬롯·구독 없음). mcp-probe 에 --real-server 모드 추가.
- Verification: 실서버 왕복 PASS — mcp-probe --real-server --daemon: omp exposure=direct:mcp__ch_ws_list / grok exposure=meta:use_tool, 둘 다 invoked·returned=true, foreign=0. 툴 10종 전부 mcp__ch_* 로 노출 확인. 게이트 그린(typecheck·test 422·lint·format), NFR-1 스모크 PASS(허용 외 커넥션 0건, 감시 폭 10). e2e 6건 + 단위 23건 + 게이트 3건 + terminal.read 3건 신규.
- Follow-up:
