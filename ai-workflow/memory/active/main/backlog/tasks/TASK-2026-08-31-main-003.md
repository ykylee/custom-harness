---
id: TASK-2026-08-31-main-003
status: done
created_at: 2026-08-31
source_anchor: generic-task-2026-08-31-main-003
source_path: backlog/2026-08-31.md
kind: generic
---

# TASK-2026-08-31-main-003 — M7 7.2.0a 하네스 홈 격리 누수 봉쇄 (NFR-1)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-31
- Owner: yklee
- Host:
- Host IP:
- Affected documents:
  - `docs/reference/harness-mcp-support.md`
  - `docs/roadmap/m7-orchestration.md`
  - `docs/roadmap/PROGRESS.md`
  - `docs/design/credential-injection-design.md`
  - `docs/design/daemon-design.md`
  - `docs/design/settings-contract.md`
  - `docs/requirements/fr2-gateway.md`

- Description: 7.2.1 실측에서 검출한 격리 누수를 봉쇄한다. omp·grok 이 `PI_CODING_AGENT_DIR`/`GROK_HOME` 격리에도 사용자 실제 `$HOME` 의 외부 MCP 설정을 읽어 서버를 띄우는 통로(NFR-1 우회)를 끊고, `nfr1-smoke` 감시를 하네스 자식 프로세스 트리까지 확장한다. 7.2.3 의 선행.
- Completion criteria: 하네스 spawn env 에서 `HOME`(win32 `USERPROFILE`)·XDG 계열이 격리 홈으로 고정되고, 외부 유입 MCP 툴이 0으로 떨어지는 대조 실측 확보
- Completion criteria: 격리로 인한 부작용(git 신원·저장소 조작) 판정 + omp·grok 왕복 무회귀 확인
- Completion criteria: `nfr1-smoke` 가 하네스의 자손 프로세스까지 감시하고 통과
- Completion criteria: 원격(http/sse) MCP 등록이 트래픽 경계 검사(FR-2.5) 위반으로 잡힘

## 🛠️ Implementation / Content

- Progress: `gateway/home-isolation.ts` 신설 — 하네스별 빈 홈 `data/harness-home/<harness>/` 생성(XDG 하위 포함), `harness.homeLinks` allowlist 항목만 실제 홈으로 심볼릭 링크, `HOME`·`USERPROFILE`(win32)·`XDG_{CONFIG,DATA,STATE,CACHE}_HOME` 오버레이 반환. 경로가 섞인 항목명은 거부.
- Progress: `GatewayService.buildEnv` 가 3하네스 모두에 오버레이를 얹는다. 격리 홈 생성 실패는 삼키지 않는다(격리는 성립하거나 세션 생성이 실패하거나 둘 중 하나). 기동 시 3하네스 격리 홈을 선준비하고 격리 off 면 경고.
- Progress: 설정 2종 신설 — `harness.homeIsolation`(기본 true, env `CUSTOM_HARNESS_HOME_ISOLATION`), `harness.homeLinks`(기본 `.gitconfig`·`.ssh`, env `CUSTOM_HARNESS_HOME_LINKS`).
- Progress: `checkTrafficBoundaries` 에 원격 MCP 등록 검사 추가 — omp `mcp.json` `mcpServers.*.url`, grok `config.toml` `[mcp_servers.*].url`. stdio 는 목적지가 없어 위반 아님.
- Progress: `scripts/nfr1-smoke.mjs` v3 — `ps -Ao pid=,ppid=` BFS 로 원장 pid 의 자손 전체를 감시 대상에 포함. `scripts/mcp-probe.mjs` 에 `--no-home-isolation` 대조군 플래그 + `foreignMcpTools` 판정축 추가.
- Next session starting point: 7.2.0b grok 권한 모드 확정(7.2.3 의 마지막 선행) 또는 7.1.1 주의 상태 1급화(선행 없음)
- Remaining risks: Windows(`USERPROFILE`·심볼릭 링크 권한)에서 동일 봉쇄가 성립하는지 미검증 — C-5 실기기 항목
- Remaining risks: 격리 홈이 비어 있어 하네스가 사용자 셸 설정(`.zshrc` 등)을 못 본다 — 현재까지 관측된 회귀는 없으나 실사용에서 반입 목록 조정이 필요할 수 있다

## ✅ Outcome

- Result: 봉쇄 완료. 대조 실측 `node scripts/mcp-probe.mjs --only omp --no-home-isolation` → `foreign=40`, 기본(격리 on) → `foreign=0`. omp·grok 왕복은 CLI·데몬 양 경로 그대로 PASS(`returned=true`), pi 는 변함없이 MCP 미지원. 격리 홈 + `.gitconfig` 링크 상태에서 `git config --global user.name/email` 정상 해석 및 저장소 `git status` 정상 — 부작용 없음. `ps` BFS 가 손자 프로세스를 포착함을 별도 확인. 게이트: typecheck·test(357 passed | 2 skipped, 37 files)·lint·format 그린, `npm run smoke:nfr1` PASS(감시 폭 8개, 허용 외 커넥션 0건).
