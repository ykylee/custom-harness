<!-- standard-ai-workflow-kit: v1.4.0 -->

# M0 사내 확인 체크리스트 (사용자 협조 필요)

- 문서 목적: M0 환경 실측 중 사내 환경 접근이 필요해 사용자 확인·협조가 필요한 항목. 확인되는 대로 답을 기입하고 PROGRESS 를 갱신한다.
- 최종 수정일: 2026-08-25

## C-1. 게이트웨이 실측 (WBS 0.5.4 — **최우선**, M1 수용 기준의 입력)

- [ ] 게이트웨이가 `GET /v1/models` (모델 목록)를 지원하는가? → FR-2.4 방식 결정 (미지원 시 정적 카탈로그 폴백)
- [ ] SSE 스트리밍 중계 품질 — `stream: true` Chat Completions 호출 시 토큰 단위로 즉시 흘러나오는가, 버퍼링 후 몰아서 오는가? (간단 확인: `curl -N` 으로 체감 확인 가능 — 명령은 요청 시 제공)
- [ ] 비표준 응답 특성 유무 — `developer` role 거부, 필드 누락 등 (pi `compat` 플래그 필요 여부)
- [ ] 사용자별 API 키 발급 절차 — 셀프서비스 포털? 관리자 신청? 발급 API 존재?
- [ ] 키 인증 방식 — `Authorization: Bearer` 인가, 커스텀 헤더인가?

## C-2. 내부 아티팩트 저장소 (WBS 0.5.2)

- [ ] 사내망에 Nexus / Artifactory / 기타 아티팩트 저장소가 운영되는가?
- [ ] 있다면: 종류·접근 방식(익명/토큰), 대용량(수백 MB) 바이너리 게시 가능 여부 → FR-4.6(저장소 연동)·M3 WP3.2 범위 확정

## C-3. 코드 서명 체계 (WBS 0.5.3)

- [ ] 조직이 macOS 개발자 인증서(공증 가능) 또는 Windows 코드서명 인증서를 보유하는가?
- [ ] 미서명 앱의 사내 배포가 정책상 허용되는가? (Gatekeeper/SmartScreen 우회 절차 안내 가능 여부) → M3 WP3.4 갈래 결정

## C-4. 크리덴셜 자동 프로비저닝 (WBS 0.4.3)

- [ ] 게이트웨이 운영 주체에게 키 발급 API/연동 계획이 있는가? (없으면 "수동 입력 확정 + 자동화 보류"로 기록하고 종결)

## C-5. (참고) Windows 실기기

- [ ] Windows x64 실기기(또는 VM) 접근 가능 여부 — 0.5.1 문서 조사 결과 검증용 실측에 필요
