---
id: TASK-2026-09-02-main-007
status: done
created_at: 2026-09-02
source_anchor: release-task-2026-09-02-main-007
source_path: backlog/2026-09-02.md
kind: release
wbs: exempt
wbs_exempt_reason: 기존 M3 범위의 UI 재설계 후속으로, 시각 방향 선택 전에는 세부 WBS를 확정할 수 없다.
---

# TASK-2026-09-02-main-007 — M3 shadcn 기반 UI 재설계

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-02
- Owner: codex
- Host:
- Host IP:
- Affected documents:
  - `packages/renderer/src`
  - `packages/renderer/package.json`
  - `scripts/serve-web-preview.mjs`
  - `design-qa.md`
  - `packages/renderer/src/views/Conversation.tsx`
  - `packages/renderer/src/views/views.test.tsx`
  - `packages/renderer/src/styles.css`
  - `packages/renderer/src/components/PermissionCard.tsx`
  - `packages/renderer/src/views/TerminalView.tsx`
  - `packages/renderer/src/views/TerminalView.test.tsx`

- Description: 현재 Custom Harness 데스크톱 앱의 UI를 shadcn 기반으로 재설계하고 점진적으로 구현한다.
- Completion criteria: 현재 UI 화면을 캡처해 시각 방향 3개를 제시하고 하나를 선택한다.
- Completion criteria: 선택 방향으로 Tailwind/shadcn 기반과 핵심 화면을 전환한다.
- Completion criteria: 사내망 IP와 Tailscale IP에서 선택된 웹 시안에 접근할 수 있다.

## 🛠️ Implementation / Content

- Progress: WorkQueue·세션 대화·승인 카드·터미널 상세를 선택된 cockpit 시각 체계로 전환해 TASK-007의 핵심 화면 범위를 완료했다. 터미널은 LIVE SHELL 컨텍스트 헤더와 식별자를 분리하고 xterm 호스트를 작업 표면으로 정리했으며, PTY attach·detach·입출력 계약은 유지했다.
- Next session starting point: 원격 live read-model 연결은 인증·권한 경계 설계가 확정된 별도 작업에서 다룬다.
- Remaining risks: 원격 미리보기는 정적 샘플 데이터이며 daemon과 연결되지 않았다.
- Remaining risks: 사설 네트워크 접근 제어는 IP 대역 기반이며, 추가 인증은 아직 적용하지 않았다.

## ✅ Outcome

- Result: 캡처 근거: 현재 UI는 세로 행동 탭과 정보 밀도가 높아, 워크벤치·대화 중심·운영 큐의 세 레이아웃 방향을 생성했다.
- Result: npm run preview:network은 빌드 산출물만 제공하며 Vite 개발 서버를 원격 노출하지 않는다.
- Result: Safari를 사내망 URL로 열어 웹 시안을 검토할 수 있게 했다.
- Result: WorkQueue 단위 테스트 2건을 새로 추가해 실제 상태 표시와 세션 열기 동작을 검증했다.
- Result: WorkQueue RED(미구현 import 실패) → GREEN(2 tests pass); npm test: 706 passed, 2 skipped (708).
- Result: Conversation 구조 단위 테스트를 RED(새 cockpit/timeline 클래스 부재)→GREEN으로 추가했다.
- Result: 승인 카드 구조 테스트를 RED(대기 상태 클래스 부재)→GREEN으로 확장했다.
- Result: TerminalView 구조 테스트를 RED(헤더와 cockpit 컨테이너 부재)→GREEN으로 추가했다.
- Verification: npm run typecheck, npm run lint, npm run format:check, npm run -w @custom-harness/renderer build, npm test: 707 passed, 2 skipped.
- Follow-up:
