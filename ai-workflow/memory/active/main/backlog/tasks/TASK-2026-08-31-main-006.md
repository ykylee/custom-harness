---
id: TASK-2026-08-31-main-006
status: done
created_at: 2026-08-31
source_anchor: generic-task-2026-08-31-main-006
source_path: backlog/2026-08-31.md
kind: generic
---

# TASK-2026-08-31-main-006 — M7 7.2.2 역방향 툴 카탈로그 정본 정의 (FR-9.2)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-08-31
- Owner: yklee
- Host:
- Host IP:
- Affected documents:
  - `docs/design/reverse-tool-catalog.md` (신규)
  - `docs/requirements/fr9-orchestration.md`
  - `docs/roadmap/m7-orchestration.md`
  - `docs/roadmap/PROGRESS.md`

- Description: 데몬이 자신의 기능을 하네스에게 되돌려 노출하는 툴 목록을 정본으로 확정한다. 이름·파라미터·효과 분류·승인 대상 여부를 고정해 7.2.3(노출 2경로)과 7.2.4(안전장치)의 전제를 만든다.
- Completion criteria: FR-9.2 가 열거한 범위를 모두 덮는 카탈로그가 코드로 존재할 것
- Completion criteria: 이름 규칙이 하네스 재접두사(omp `mcp__<s>_<t>` / grok `<s>__<t>`)를 견딜 것
- Completion criteria: MCP `tools/list` 항목으로 변환 가능하고 pi 확장과 공용일 것

## 🛠️ Implementation / Content

- Progress: `packages/protocol/src/tools.ts` 신설 — 노출 경로가 둘(omp·grok = 데몬 MCP 서버 / pi = `pi.registerTool` 확장)이라 각자 정의하면 하네스마다 다른 툴 표면이 생긴다. 그래서 정의를 양쪽보다 위층인 프로토콜 패키지에 둔다.
- Progress: 툴 10종 — `session_list`(주의 상태 동봉)·`session_read`·`session_new`(재귀 위험)·`session_say`·`session_stop`·`ws_list`·`term_list`·`term_new`·`term_send`·`term_read`. 이름은 `TOOL_NAME_PATTERN`(소문자·숫자·`_`, 3~24자)으로 고정.
- Progress: `ToolSpec.effect`(read/write) + `approval` + `spawnsSession`. **승인은 우리가 소유한다** — 하네스는 우리 MCP 서버를 한 덩어리로만 봐서 "목록 조회"와 "임의 셸 입력"을 구분하지 못한다(grok 은 서버 단위 `always-allow` 까지 제안, 7.2.0b 실측). `write` 는 전부 승인 대상, `read` 는 승인 불요(조회까지 막으면 위임 세션 감시가 불가능)를 테스트로 고정.
- Progress: 파라미터는 `z.object`(엄격) — 와이어 스키마의 `looseObject` 관례를 따르지 않는다. 그 관례는 번들 버전 간 관용을 위한 것인데 카탈로그와 핸들러는 같이 배포되고, 여기 입력을 만드는 쪽은 모델이라 환각 파라미터가 조용히 무시되면 안 된다.
- Progress: `toolDescriptor()` 가 zod → JSON Schema(2020-12) 변환(zod 4 `z.toJSONSchema`). MCP `tools/list` 와 pi 확장이 같은 산출물을 쓴다. 설계서 `docs/design/reverse-tool-catalog.md` 신설.
- Next session starting point: 7.2.3 노출 2경로 구현 — 데몬 소유 MCP stdio 서버(omp·grok) + pi 확장, 등록 절차, omp rpc 경로 MCP 준비 완료 게이트, 핸들러의 데몬 RPC 바인딩
- Remaining risks: 툴 **결과** 스키마는 미고정 — 핸들러가 데몬 RPC 응답을 어디까지 그대로 흘릴지가 7.2.3 에서 정해진다
- Remaining risks: `term_send` 승인 시 사용자에게 무엇을 보여줄지(입력 원문 전체 vs 요약)는 7.2.4 UI 항목

## ✅ Outcome

- Result: 카탈로그 확정. FR-9.2 열거 범위 6종(세션 생성·프롬프트 전송·상태 조회·중단·워크스페이스 조회·터미널 조작)을 모두 덮고, 그 대응을 테스트가 고정한다. `session_list` 는 7.1 이 계산한 주의 상태를 그대로 싣는다(툴 층에서 재판단 금지 — 7.1 이 없앤 문제를 되살리지 않는다). 게이트: typecheck·test(**385 passed | 2 skipped**, +9건)·lint·format 그린.
