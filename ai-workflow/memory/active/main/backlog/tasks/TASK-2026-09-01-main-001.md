---
id: TASK-2026-09-01-main-001
status: done
created_at: 2026-09-01
source_anchor: generic-task-2026-09-01-main-001
source_path: backlog/2026-09-01.md
kind: generic
wbs: 7.2.3b
---

# TASK-2026-09-01-main-001 — M7 7.2.3b 역방향 툴 노출 — pi 확장 (pi.registerTool)

## 📝 Description

- Status: done
- Priority: high
- Request date: 2026-09-01
- Owner:
- Host:
- Host IP:
- Affected documents:
  - `docs/design/reverse-tool-catalog.md`
  - `docs/roadmap/m7-orchestration.md`
  - `docs/roadmap/PROGRESS.md`

- Description: pi 는 MCP 를 설계상 배제하므로 같은 카탈로그를 확장으로 노출한다. 확장이 우리 MCP 서버를 자식으로 띄워 카탈로그·승인 게이트·바인딩을 그대로 재사용한다.
- Completion criteria: pi 확장이 격리 홈에서 자동 로드되고 카탈로그 10종이 pi.registerTool 로 등록 / 실물 pi 왕복(등록→노출→호출→결과 반영) PASS / 게이트 그린 / NFR-1 스모크 PASS

## 🛠️ Implementation / Content

- Progress: `2026-09-01 07:37` 기준 pi 는 MCP 를 설계상 배제하므로 같은 카탈로그를 확장으로 노출한다. 확장이 우리 MCP 서버를 자식으로 띄워 카탈로그·승인 게이트·바인딩을 그대로 재사용한다.
- Next session starting point: 7.2.4 — 승인 채널(write 5종이 여기서 열린다)·opt-in 기본 off·spawnsSession 재귀 상한·감사 로그·서버명 선점 탐지
- Remaining risks: 번들 패키징에 pi-extension/ 복사가 아직 없다 — dist 만 복사하면 실기기에서 확장이 설치되지 않는다(M3 패키징 항목). 실측 부수 확인: parameters 에 생 JSON Schema 가 통해 TypeBox 변환층 불필요.

## ✅ Outcome

- Result: packages/daemon/pi-extension/reverse-tools.ts 신설(우리 tsc 대상 아님 — pi 가 컴파일, node 내장만 의존). 확장은 카탈로그를 모르고 MCP 서버 자식의 tools/list 결과를 그대로 pi 로 옮긴다. registerPiExtension()이 $PI_CODING_AGENT_DIR/extensions/ch-reverse-tools.ts 로 매번 덮어쓰고, spawn 사양은 piExtensionEnv()로 env 전달(파일에 굽지 않음). startDaemon 의 buildEnv 에 pi 한정 오버레이. resolveMcpServerSpec 이 CUSTOM_HARNESS_MCP_LOG 를 물려줌(별도 프로세스라 데몬 로그만으로는 진단 불가). mcp-probe --real-server 가 pi 를 확장 경로로 검증.
- Verification: 실물 왕복 PASS — mcp-probe --real-server --daemon --only pi: initialized·registered·invoked·returned 전부 true, exposure=direct:ws_list, 툴 10종 등록 확인, toolResult={"workspaces":[]}, foreign=0. 게이트 그린(typecheck·test 425·lint·format). NFR-1 PASS(허용 외 커넥션 0건, 감시 폭 12 — MCP 자식까지 포함). 신규 테스트 3건.
- Follow-up:
